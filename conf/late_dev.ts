
import { LateConfig } from '../app/lateconfig';

const config: LateConfig = {
  id: 'aidchess',
  project: 'AidChess',
  cookie_secret: 'dev',
  lila_oauth: {
    client_id: 'aasfd',
    client_secret: 'adsflkj'
  }
};

export default config;
